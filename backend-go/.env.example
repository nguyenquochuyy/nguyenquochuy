# UniShop Go Backend — Environment Variables
# Copy to .env and fill in your values

# MongoDB
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
DB_NAME=unishop

# JWT
JWT_SECRET=change-me-to-a-long-random-secret

# Server
PORT=8080
APP_ENV=development

# Redis (để trống nếu không dùng Redis — sẽ fallback sang in-memory cache)
REDIS_URL=redis://localhost:6379

# Email (Gmail App Password)
EMAIL_USER=nhuy0852004@gmail.com
EMAIL_PASS=xowh ldgs adru ngmv
